type: break
break:
  description: |-
    The `TaggedMetricsScheduledExecutorService` no longer reports the `executor.scheduled.percent_of_period` histogram.
    It was observed that this metric is not actually being used.
  links:
  - https://github.com/palantir/tritium/pull/505
